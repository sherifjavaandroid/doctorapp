import '../../../utils/basic_screen_imports.dart';

class SetupLocationScreenMobile extends StatelessWidget {
  const SetupLocationScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}